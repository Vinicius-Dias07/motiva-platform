[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["backend"]
testpaths = ["backend/tests"]

[tool.ruff]
line-length = 100
target-version = "py313"
extend-exclude = ["backend/model"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
